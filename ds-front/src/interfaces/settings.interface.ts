export interface Settings {
  _id: string;
  colors: Colors;
}

export interface Colors {
  // topBarColor: string;
  // backgroundColor: string;
  iconColor: string;
  textColor: string;
  completeBtnColor: string;
}
